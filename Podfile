# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'RecipeApi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecipeApi
  pod 'Alamofire', '~> 4.0'
  target 'RecipeApiTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
